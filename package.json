{
  "name": "tabor",
  "version": "0.0.0",
  "private": true,
  "description": "TABOR — Sons of Fire. Gamified brotherhood app for Christian men.",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "test": "npm run test --workspaces --if-present",
    "typecheck": "npm run typecheck --workspaces --if-present",
    "lint": "npm run lint --workspaces --if-present"
  },
  "engines": {
    "node": ">=20"
  },
  "devDependencies": {
    "supabase": "^2.105.0"
  }
}
