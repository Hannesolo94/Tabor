# apps/web

Next.js (App Router) storefront + marketing site + `/admin`. Deploys to Vercel at
`tabor.quest`. Built in **Phase 2** of `docs/BUILD-PLAN.md`.

Rebuilds `TABOR Website.html` / `site-store.jsx` on the production stack. Imports brand
tokens and game logic from `@tabor/shared`.

Not scaffolded yet. Phase 0 (the shared package) comes first.
