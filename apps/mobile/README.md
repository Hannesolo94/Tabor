# apps/mobile

React Native + Expo app. Previewed on a phone via Expo Go (QR), built with EAS.
Built in **Phase 3+** of `docs/BUILD-PLAN.md`.

Ports the prototype screens (`proto-*.jsx`, see `docs/SCREENS.md`) onto the production
stack. Imports brand tokens and game logic from `@tabor/shared`.

Not scaffolded yet. Phase 0 (the shared package) comes first.
