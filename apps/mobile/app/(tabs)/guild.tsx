import { Pillar } from "@/components/Pillar";

export default function Guild() {
  return (
    <Pillar
      tag="BROTHERHOOD"
      title="The Guild"
      intro="No man climbs alone. Real-time guild channels, the roster, the leaderboard, and the monthly giveaway."
      points={["Guild channels + direct messages", "Roster and roles", "Leaderboard", "Monthly giveaway voting"]}
    />
  );
}
