import { Pillar } from "@/components/Pillar";

export default function Body() {
  return (
    <Pillar
      tag="FITNESS GUILD"
      title="The Body"
      intro="Train the temple. Routines, a workout player, and the Tabata timer. Log the work, watch the climb."
      points={["Routines + workout player", "Tabata builder and timer", "History + personal records", "Honor the streak"]}
    />
  );
}
