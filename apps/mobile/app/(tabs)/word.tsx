import { Pillar } from "@/components/Pillar";

export default function Word() {
  return (
    <Pillar
      tag="SCRIPTURE RAID"
      title="The Word"
      intro="Daily scripture, reading plans, and prayer. Take ground in the Word every day."
      points={["Today's passage + reflection", "Reading plans (the campaign)", "Bookmarks and prayers", "Seeker Track for those still asking"]}
    />
  );
}
